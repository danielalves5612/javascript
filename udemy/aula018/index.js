/*const pessoa1 = {
    nome: 'Daniel',
    sobrenome: 'Alves',
    idade: 20,

    fala() {
        console.log(`A minha idade atual é ${this.idade}`)
    },

    incrementaIdade() {
        this.idade++ -> Você pode usar o this, para referenciar os parametros do objeto, nesse caso ele mostra a idade depois    soma mais 1
    }
}

pessoa1.fala()
pessoa1.incrementaIdade()
pessoa1.fala()
pessoa1.incrementaIdade()
pessoa1.fala()
pessoa1.incrementaIdade()
pessoa1.fala()
*/

/*
function criaPessoa (nome, sobrenome, idade){
    return {nome, sobrenome, idade} // Você não precisa colocar nome = valor do nome e etc, basta apenas colocar o parametro
}

const pessoa1 = criaPessoa('Daniel', 'Alves', 20)
const pessoa2 = criaPessoa('Isabel', 'Fernanda', 23)
const pessoa3 = criaPessoa('Magali', 'Aparecida', 62) -> Você pode usar uma função, para criar objetos, ela é chamada de factory
const pessoa4 = criaPessoa('Anilton', 'Souza', 61)
console.log(pessoa1.nome, pessoa2.nome)
*/

/*
const pessoa1 = {
    nome: 'Daniel',
    sobrenome: 'Alves de Souza', // Para informar valores de um obj, basta informa a caracteristica e o valor, separando por ','
    idade: 20,
}

console.log(pessoa1.nome)
console.log(pessoa1.sobrenome)
console.log(pessoa1.idade)
*/